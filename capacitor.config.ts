import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'curso-ionic-cadmus',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
